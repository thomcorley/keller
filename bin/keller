#!/usr/bin/env ruby
require_relative "../lib/cli"

ingredients = ARGV
cli = Cli.new

case
when ingredients.none?
  cli.sample
when ingredients.count > 4
  puts "Error: Keller can't look up recipes with more than 4 ingredients"
else
  cli.lookup_recipes(ingredients: ingredients)
end
